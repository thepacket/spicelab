[package]
name = "spicelab-wasm"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "wasm-bindgen boundary for spicelab-core. Contains no numerics."

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
spicelab-core = { path = "../spicelab-core" }
wasm-bindgen = "0.2"
